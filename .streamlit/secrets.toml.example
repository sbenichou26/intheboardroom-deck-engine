# Copy this file to secrets.toml locally, and set the same two keys in
# Streamlit Community Cloud (App -> Settings -> Secrets). Never commit the
# real secrets.toml (it is gitignored).
ANTHROPIC_API_KEY = "sk-ant-..."
APP_PASSWORD = "choose-a-strong-password"
